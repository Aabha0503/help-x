# Docker Infrastructure

Docker-related production and development configuration belongs here.

Use root `docker-compose.yml` for local development and add production compose overrides only when needed.
