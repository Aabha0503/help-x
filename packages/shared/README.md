# Shared Package

Common constants, type contracts, and validation rules used by web, mobile, backend, and services.

This prevents each module from inventing different names for incident statuses, telemetry fields, roles, and event types.
